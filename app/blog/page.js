import Layout from '@/components/layout';

export const metadata = {
    title: "blog - NextJS",
    description: "Guitars, sale guitars, market place guitars, nextjs app",
};

const Blog = () => {
    return (
        <Layout>
            <h2 className='title-app'> Blog</h2>
        </Layout>
    );
}

export default Blog;
